FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml uv.lock service_config.yaml /app/
RUN pip install -r /app/uv.lock || true
COPY src /app/src
COPY models /app/models
CMD ["uvicorn", "src.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
